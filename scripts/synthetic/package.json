{
  "name": "geetanjali-synthetic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Outside-in production checks. Kept out of frontend/ so Playwright never enters the app's dependency tree.",
  "scripts": {
    "check": "node check.mjs"
  },
  "dependencies": {
    "playwright": "^1.56.0"
  }
}
